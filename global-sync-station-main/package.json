{
  "name": "ntp-server",
  "version": "1.0.0",
  "description": "NTP Server - Global Sync Station",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm --prefix ./frontend run dev",
    "build": "npm --prefix ./frontend run build",
    "preview": "npm --prefix ./frontend run preview"
  }
}
